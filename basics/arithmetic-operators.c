#include <stdio.h>

int main() {
    //artihmetic operaters = + - / % ++ -- 

    int x = 10;
    int y = 3;
    int z = 0;

    //z = x + y;
    //z = x - y;
    //z = x * y;
    //z = x / y;
    //z = x % y;

    // augmented assignment operators. (shortcuts if you're reassigning the same variable)
    //x+=2; // = 12 because x = x + 2
    //x-=2;
    //x*=2;
    //x/=2;
    //

    printf("%d", x);

}